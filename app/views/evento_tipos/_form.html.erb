<%= form_for(@evento_tipo) do |f| %>
  <% if @evento_tipo.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@evento_tipo.errors.count, "error") %> prohibited this evento_tipo from being saved:</h2>

      <ul>
      <% @evento_tipo.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :nome %><br />
    <%= f.text_field :nome %>
  </div>
  <div class="field">
    <%= f.label :icone %><br />
    <%= f.text_field :icone %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
